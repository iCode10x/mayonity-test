export type SidebarLinkType = {
  label: string;
  icon: string;
};

export type CategoryElementType = {
  image: string;
  label: string;
  numOfProducts: number;
  date: string;
  status: string;
  index?: number;
};
